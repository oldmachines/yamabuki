//! SingleStepTests 65816 harness. Placeholder until the CPU lands (M2).

const std = @import("std");

pub fn main() !void {
    std.debug.print("sst-65816: CPU core not implemented yet (M2)\n", .{});
    std.process.exit(1);
}
